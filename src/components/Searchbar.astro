---
import "../styles/global.css";

---

<div class="searchicon"> <img src="../../public/icons/searchicon.svg" alt=""></div>
<!-- <div class="searchbar hide"> 
    <input type="text" placeholder="søg...">
     <img src="../../public/icons/close.svg" alt="close"> 
    </div> -->

<style>
/* .active {
   right: 20%;
   transition: 0.2 ease-in-out;
} */
  .searchicon {
    width: 1rem;
    margin-right: 0.5rem;
    cursor: pointer;
  }

  /* .hide{
    position: absolute;
        right:-100%;
  } */ 
    /* .searchbar{
        position: absolute;
        right: 1rem;
        display: flex;
        justify-content: space-between;
        transition: 0.2 ease-in-out;
    }
    .searchbar img{
width: 1rem;
    }
    input[type=text] {   
    color: var(--text-color);
}
input:focus {   
    outline: none;
}

    input{
        border: 1px solid var(--background-color);
        border-bottom: 1px solid var(--text-color);
        background-color: var(--background-color);
        color: var(--text-color);
        font-size: 1rem;
        width: 50%;
        padding: 1rem;
    } */
</style>